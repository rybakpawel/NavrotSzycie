import React from 'react';

const AdminSection = () => {
    const checkAction = action => {
        switch (action) {
            case 'hero':
                return

            case 'aboutme':
                return

            default:
                break;
        }
    }
    return (
        <h1>section</h1>
    )
};

export default AdminSection;